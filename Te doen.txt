Optimalisatie:

1. Bij re-render van components geeft Vue aan dat this.user undefined is, dit heeft betrekking op volgorde van inladen.



kladblok:
    const mappedMealIngredients = meals.map(meal => meal.ingredient_id.map(ingredient => ingredient.id));